# 3.3 Navigation Avancée

## Redirection

```jsx
import { Navigate } from 'react-router-dom'

// Redirection simple
<Route path="/old" element={<Navigate to="/new" replace />} />
```

## Protection de routes (Auth)

```jsx
import { Navigate, Outlet } from 'react-router-dom'

function ProtectedRoute({ isLoggedIn }) {
  if (!isLoggedIn) {
    return <Navigate to="/login" replace />
  }
  return <Outlet />
}

<Routes>
  <Route element={<ProtectedRoute isLoggedIn={true} />}>
    <Route path="/admin" element={<Admin />} />
  </Route>
</Routes>
```

## Query strings

```jsx
import { useSearchParams } from 'react-router-dom'

function Search() {
  const [searchParams] = useSearchParams()
  const query = searchParams.get('q')

  return <p>Recherche: {query}</p>
}

// URL: /search?q=react
```

---

**[← Previous](3.2-parametres-url.md) | [Suivant: Passer des Données entre Pages →](3.4-passer-donnees-entre-pages.md)**

# 5.3 Layouts & Structures

## Layout principal

```jsx
import { useState } from 'react'

function MainLayout({ children }) {
  return (
    <div className="layout">
      <Header />
      <Sidebar />
      <main className="content">{children}</main>
      <Footer />
    </div>
  )
}
```

## Grid layout

```jsx
function GridLayout({ items, columns = 3 }) {
  return (
    <div style={{ display: 'grid', gridTemplateColumns: `repeat(${columns}, 1fr)`, gap: '20px' }}>
      {items.map((item, i) => (
        <div key={i}>{item}</div>
      ))}
    </div>
  )
}
```

---

**[← Previous](5.2-composants-interactifs.md)**
